<<<<<<< HEAD
# template-financeiro
=======
# 💰 Planner Financeiro Full-Stack com Streamlit e Supabase

Um planner financeiro completo desenvolvido como uma aplicação de página única (SPA) utilizando Streamlit no frontend e Supabase como backend, banco de dados (PostgreSQL) e autenticação de usuários.

A aplicação permite que múltiplos usuários gerenciem suas finanças pessoais, com segurança garantida pelo Row Level Security (RLS) do Supabase — cada usuário acessa apenas seus próprios dados.

## 🚀 Demonstração

📸 [Adicione aqui um screenshot ou GIF do seu dashboard]

> image.png

## ✨ Funcionalidades Principais

- 🔐 **Autenticação de Usuários**: Login e cadastro completos via Supabase Auth
- 🧱 **Segurança (RLS)**: Cada usuário só visualiza e manipula suas próprias transações
- 📊 **Dashboard de Página Única**: Toda a navegação (login, dashboard e gráficos) ocorre em uma única página
- 💡 **KPIs Totais**: Exibe receita, despesa, saldo e investimentos acumulados
- 📅 **KPIs Mensais**: Resumo filtrado por mês e ano selecionados
- 📈 **Gráfico de Tendência Histórica**: Mostra o fluxo de caixa (receitas, despesas e investimentos) e o saldo acumulado ao longo do tempo
- 🥧 **Gráficos de Pizza**:
  - **Despesas do Mês**: Distribuição de gastos do mês filtrado
  - **Investimentos (Geral)**: Distribuição total dos investimentos (sem filtro)
- 🧾 **CRUD de Transações**: Formulário expansível para adicionar, editar e excluir receitas, despesas e investimentos
- 📋 **Histórico Mensal**: Tabela interativa com todas as transações do mês selecionado

## 🛠️ Stack Tecnológica

| Camada | Tecnologia |
|--------|------------|
| Frontend | Streamlit |
| Backend & Banco de Dados | Supabase (PostgreSQL) |
| Autenticação | Supabase Auth |
| Análise de Dados | Pandas |
| Visualização de Dados | Plotly |
| Gerenciamento de Dependências | Poetry |

## ⚙️ Instalação e Setup

Siga os passos abaixo para rodar o projeto localmente:

### 1. Pré-requisitos

- Python 3.10+
- Poetry (para gerenciar dependências)
- Conta gratuita no Supabase

### 2. Clonar o Repositório

```bash
git clone git@github.com:Pacodeoliv/template-financeiro.git
cd template-financeiro
```

### 3. Instalar Dependências

```bash
poetry install
```

### 4. Configurar o Supabase

1. **Crie um novo projeto no Supabase**

2. **Desative a confirmação de e-mail (modo dev)**:
   - Vá em `Authentication` → `Providers` → `Email`
   - Desmarque "Confirm email"

3. **Crie a tabela `transactions`**:
   - Vá em `SQL Editor` → `New query`
   - Cole e execute o script abaixo:

```sql
-- 1. CRIAR A TABELA 'transactions'
CREATE TABLE transactions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) NOT NULL,
  tipo TEXT NOT NULL, -- 'receita', 'despesa' ou 'investimento'
  valor DECIMAL(10, 2) NOT NULL,
  descricao TEXT NOT NULL,
  categoria TEXT,
  data DATE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. HABILITAR ROW LEVEL SECURITY (RLS)
ALTER TABLE transactions ENABLE ROW LEVEL SECURITY;

-- 3. CRIAR POLÍTICAS DE ACESSO (Multi-usuário)
CREATE POLICY "Enable read access for own users"
ON transactions FOR SELECT
USING (auth.uid() = user_id);

CREATE POLICY "Enable insert access for own users"
ON transactions FOR INSERT
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Enable update access for own users"
ON transactions FOR UPDATE
USING (auth.uid() = user_id)
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Enable delete access for own users"
ON transactions FOR DELETE
USING (auth.uid() = user_id);
```

### 5. Configurar Variáveis de Ambiente

1. No painel do Supabase, acesse:
   - `Project Settings` → `API`
   - Copie:
     - **Project URL**
     - **Anon public key**

2. Crie o arquivo `.streamlit/secrets.toml` e adicione:

```toml
# .streamlit/secrets.toml
SUPABASE_URL = "SUA_URL_DO_PROJETO_SUPABASE"
SUPABASE_KEY = "SUA_CHAVE_ANON_PUBLICA"
```

> **⚠️ Importante**: O arquivo `secrets.toml` já está no `.gitignore` e não será commitado. Nunca compartilhe suas chaves de API publicamente!

### 6. Executar o Aplicativo

```bash
# Ativar o ambiente virtual
poetry shell

# Rodar o Streamlit
streamlit run app.py
```

Ou, se preferir sem ativar o shell:

```bash
poetry run streamlit run app.py
```

### 7. Modo de Desenvolvimento (Opcional)

Para pular o login durante o desenvolvimento local, edite o arquivo `app.py` e configure:

```python
DEV_MODE = True
FAKE_USER_ID = "SEU_UID_DO_SUPABASE"  # UID do usuário de teste
FAKE_USER_EMAIL = "test@test.com"     # Email do usuário de teste
```

> **⚠️ Lembre-se**: Defina `DEV_MODE = False` antes de fazer deploy em produção!

## 📁 Estrutura do Projeto

```
template-financeiro/
├── .streamlit/
│   └── secrets.toml        # Chaves de API (NÃO ENVIAR PARA O GIT)
├── app.py                  # Aplicação principal (UI, lógica e gráficos)
├── supabase_client.py      # Funções helper (Auth, CRUD)
├── style.css               # Estilos customizados
├── pyproject.toml          # Dependências (Poetry)
├── poetry.lock             # Lock file do Poetry
├── requirements.txt        # Dependências alternativas (pip)
├── .gitignore              # Arquivos ignorados pelo Git
└── README.md               # Documentação do projeto
```

## ☁️ Deploy no Streamlit Cloud

1. **Envie o projeto para um repositório no GitHub**
   - Certifique-se de que `.streamlit/` está no `.gitignore`
   - Faça push do código

2. **Acesse [share.streamlit.io](https://share.streamlit.io/)**

3. **Clique em "New app"** e selecione seu repositório

4. **Configure as secrets**:
   - Em `Advanced settings...`
   - Adicione as seguintes secrets:
     - `SUPABASE_URL`: Sua URL do projeto Supabase
     - `SUPABASE_KEY`: Sua chave anon pública do Supabase

5. **Clique em "Deploy!"** 🚀

## 🔒 Segurança

- ✅ Row Level Security (RLS) habilitado no Supabase
- ✅ Autenticação via Supabase Auth
- ✅ Chaves de API não commitadas no repositório
- ✅ Cada usuário acessa apenas seus próprios dados

## 📝 Notas Importantes

- O arquivo `.streamlit/secrets.toml` nunca deve ser commitado
- Certifique-se de que o RLS está habilitado antes de fazer deploy
- Altere as credenciais se o arquivo `secrets.toml` foi exposto acidentalmente
- Em produção, mantenha `DEV_MODE = False` no código

## 🤝 Contribuindo

Contribuições são bem-vindas! Sinta-se à vontade para abrir issues ou pull requests.

## 📄 Licença

Este projeto está sob licença MIT.

## 👤 Autor

**Paco de Oliveira Saavedra**
- GitHub: [@Pacodeoliv](https://github.com/Pacodeoliv)

---

⭐ Se este projeto foi útil para você, considere dar uma estrela no repositório!
>>>>>>> gemini-interface-claude
